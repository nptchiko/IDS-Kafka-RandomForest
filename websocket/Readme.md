### This is module create socket 

This module use socket io to create socket with UI
Run this here after mongodb and before UI

## Run command
___Navigate here___

Make sure install all library in requirement
Or run command here

```
pip install -r requirement.txt
```

```
python3 server.py
```